const initialState = {
  user: {},
  location: '',
  cart: {},
  error: null
}

export const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
